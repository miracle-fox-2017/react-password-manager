const addPassword = {
  type: 'ADD_PASSWORD',
  payload: []
}

const getPassword = {
  type: 'GET_PASSWORD'
}

const removePassword = {
  type: 'REMOVE_PASSWORD',
  payload
}

const actions = {
  addPassword
}

export default actions
